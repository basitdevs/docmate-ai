import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { AuthProvider } from "@/context/AuthContext";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "DocMate Pro - AI Medical Transcription & Clinical Notes",
  description:
    "Professional AI-powered medical conversation transcription with intelligent clinical notes generation in SOAP format. Secure, accurate, and HIPAA-compliant healthcare documentation.",
  keywords:
    "medical transcription, clinical notes, SOAP format, healthcare AI, doctor patient conversation, medical documentation, AI transcription",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang='en'>
      <body className={`${geistSans.variable} ${geistMono.variable} antialiased`}>
        <AuthProvider>{children}</AuthProvider>
      </body>
    </html>
  );
}
